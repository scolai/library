/* 
transition: background-color 0.5s ease; 

/*
.input-wrapper:focus-within .input-icon::before {
	background-color: var(--gray-dark); }



transition: box-shadow var(--transition-default) ease; 

/* :focus
box-shadow: 0px 1px 9px 0px var(--gray-light);

/* overhead border
box-shadow: 0px 2px 0px -1px var(--gray-darkest) inset;

/* raised bottom border .. (border raises under fixed border)
box-shadow: 0px 2px 0px -1px var(--gray-darkest); 

/* underline without pushing up content (thin underline)
a border 1px transparent
a:hover border 1px ...

